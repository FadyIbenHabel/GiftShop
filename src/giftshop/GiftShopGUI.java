/*package giftshop;
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;

public class GiftShopGUI extends Application {

    public static void main(String[] args) {
        launch(args);
    }

    @Override
    public void start(Stage primaryStage) {
        primaryStage.setTitle("Gift Shop");

        // Create GUI components
        Button addTransactionButton = new Button("Add Transaction");
        // Add event handler for the button to handle adding transactions

        Button displayInventoryButton = new Button("Display Inventory");
        // Add event handler for the button to display inventory

        VBox layout = new VBox(10);
        layout.getChildren().addAll(addTransactionButton, displayInventoryButton);

        Scene scene = new Scene(layout, 300, 200);
        primaryStage.setScene(scene);

        primaryStage.show();
    }
}*/
